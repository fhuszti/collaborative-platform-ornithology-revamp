{{ form_start(form, {
	'method': 'post',
	'action': path('fos_user_profile_edit'),
	'attr': {
		'class': 'fos_user_profile_edit form-horizontal'
	}
}) }}
    {% if form_errors(form.email) %}
        {{ form_errors(form.email) }}
    {% endif %}
    {% if form_errors(form.current_password) %}
        {{ form_errors(form.current_password) }}
    {% endif %}
    {% if form_errors(form) %}
        {{ form_errors(form) }}
    {% endif %}

    <div class="form-group">
        {{ form_label(form.email) }}
        <div class="col-sm-8">
        	{{ form_widget(form.email) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.current_password) }}
        <div class="col-sm-8">
        	{{ form_widget(form.current_password) }}
        </div>
    </div>
    
    <div class="form-group" id="profile_email-modal-submit-group">
        <button type="submit" class="btn btn-info" id="profile_email-modal-button" data-loading-text="<span class='glyphicon glyphicon-repeat'></span> En cours de validation&#8230;">
        	{{ 'core.profile.block1.content.edit'|trans|upper }}
        </button>
    </div>
{{ form_end(form) }}
